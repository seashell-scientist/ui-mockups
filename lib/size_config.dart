import 'package:flutter/widgets.dart';

class SizeConfig {
  /*
  static late MediaQueryData _mediaQueryData;
  static late double screenWidth;
  static late double screenHeight;
  static late double blockSizeHorizontal;
  static double? blockSizeVertical;

  static double? mediumFontSize;
  static double? smallFontSize;
  static double? largeFontSize;
  static double? largestFontSize;

  void init(BuildContext context) {
    _mediaQueryData = MediaQuery.of(context);
    screenWidth = _mediaQueryData.size.width;
    screenHeight = _mediaQueryData.size.height;
    blockSizeHorizontal = screenWidth / 100;
    blockSizeVertical = screenHeight / 100;

    smallFontSize = blockSizeHorizontal * 2;
    mediumFontSize = blockSizeHorizontal * 3.5;
    largeFontSize = blockSizeHorizontal * 5;
    largestFontSize = blockSizeHorizontal * 7;
  }

  static Size getSize(BuildContext context) => MediaQuery.of(context).size;
*/

}
